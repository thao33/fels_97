<%= form_for current_user.active_relationship.find_by(followed_id: @user.id),
              html: {method: :delete}, remote: true do |f| %>
  <%= f.submit t("profile.button.unfollow"), class: "btn" %>
<% end %>