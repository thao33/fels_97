<%= form_for current_user.active_relationship.build, remote: true do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit t("profile.button.follow"), class: "btn btn-primary" %>
<% end %>